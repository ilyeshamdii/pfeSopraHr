import { SortByTimes2Pipe } from './sort-by-times2.pipe';

describe('SortByTimes2Pipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTimes2Pipe();
    expect(pipe).toBeTruthy();
  });
});
