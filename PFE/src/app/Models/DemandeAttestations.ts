export interface DemandeAttestations {
    id?: number;
    user_id: number; // Assuming this represents the user ID
    attestation_id: number; // Assuming this represents the attestation ID
    isApproved?:string
  }
  